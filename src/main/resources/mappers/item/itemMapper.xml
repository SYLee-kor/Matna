<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="item">
	<!-- 아이템상세보기 -->
	<select id="detail" resultType="item">
		select photo, name, price, amount, regdate, content
		from item
		where no=#{no}
	</select>
	
	<!-- 아이템구매시 수량감소 -->
	<update id="update">
		update item
		set amount=amount+#{num}
		where no=#{no}
	</update>
	
	<!-- 전체 아이템 리스트 -->
	<select id="listAll" resultType="item">
		select no, photo, name, price
		from item
		order by no desc
	</select>
</mapper>
